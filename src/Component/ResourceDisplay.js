import React from 'react'

const ResourceDisplay = (requestData) => {

    return(
        <div className="request-output">
            {requestData}
        </div>
    )

}

export default ResourceDisplay